#!/usr/bin/env bash
# @BP010: Release metadata
# @package: mush-embed
# @build_type: bin
# @build_with: Mush 0.2.0 (2025-01-05 develop)
# @build_date: 2025-01-31T18:21:35Z
set -e
use() { return 0; }
extern() { return 0; }
legacy() { return 0; }
module() { return 0; }
public() { return 0; }
embed() { return 0; }
## BP004: Compile the entrypoint

embed pluto
embed alias_for_pluto pluto
embed manifest ../Manifest.toml

main() {
  pluto
  alias_for_pluto
  manifest
}
pluto() {
  cat <<'EOF'
pluto() {
  echo "Pluto is a dwarf planet."
}
EOF
}
## BP005: Execute the entrypoint
main "$@"
